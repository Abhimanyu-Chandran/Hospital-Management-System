# TODO

- [x] Add shared UI utility classes to `client/index.css` (buttons, cards/panels, inputs, modal, table rows, toasts, filter chips) using CSS variables for consistent radius/shadows.

- [ ] Refactor JSX across modules to use the shared classes (Dashboard, Patients, Appointments, Doctors, MedicalRecords, SideBar, HomePage).


- [ ] Normalize inconsistent usages (remove duplicate/contradicting classes like `border-2 rounded-md rounded-md`, unify hover/disabled behavior).
- [x] Run build/lint to ensure everything compiles.


